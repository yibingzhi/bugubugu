import { DirectiveBinding } from "vue";
/**
 * 转换字符串，为null转换为空字符串
 * @param val
 * @returns
 */
export declare const str: <T>(val: T) => string;
/**
 * 判断是否是图片类型
 * @param filePath
 * @returns
 */
export declare function isImage(filePath: string): boolean;
export declare function createObjectURL(blob: any): string;
/**
 * 移动元素位置
 * @param array 数组
 * @param oldIndex 旧索引
 * @param newIndex 新索引
 * @returns
 */
export declare const moveElement: (array: any[], oldIndex: number, newIndex: number) => any[];
/**
 * 根据对象数组中某一属性的值删除
 * @returns
 */
export declare const removeByValue: (arr: any[], attr: string, value: any) => void;
/**
 * 创建自然数组
 * @param start
 * @param end
 * @returns
 */
export declare function createNaturalNumArr(start: number, end: number): number[];
/**
 * 返回像素
 * @param val
 * @returns
 */
export declare const toPx: (val: any) => any;
export declare function mergeObject(target: any, source: any): void;
export declare const vClickOutside: {
    beforeMount(el: any, binding: DirectiveBinding): void;
    unmounted(el: any): void;
};
